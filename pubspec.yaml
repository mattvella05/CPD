name: matthew_cpd_assignment
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.12.1
  firebase_database: ^11.3.4
  google_maps_flutter: ^2.1.0
  geolocator: ^13.0.2
  flutter_local_notifications: 16.1.0
  provider: ^6.0.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
